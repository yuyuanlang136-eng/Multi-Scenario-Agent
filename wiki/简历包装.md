# Bear Agent 简历包装指南

本文档用于把 Bear Agent 包装成可以直接写进简历、项目经历、面试自我介绍和答辩材料的版本。新版表达重点是：**Bear Code 是一个自研的自进化 Harness Agent，而不是普通 CLI 聊天工具**。

核心主线建议固定为：

```text
自研 Agentic Harness
  -> 统一编排模型、工具、权限、上下文和持久化
  -> 支持自主会话记忆折叠、Skills 自进化、MCP、子 Agent
  -> 通过 Memory 折叠 + Skills 沉淀支撑长程任务
  -> 在线评测 Skills：replay、规则、LLM judge、candidate 试跑、champion 记录
  -> 形成可执行、可沉淀、可审计、可扩展、适合长程任务的 Coding Agent Runtime
```

## 1. 项目名称包装

### 中文名




**Bear Agent：集成自主会话记忆折叠、Skills 自进化、MCP 与子 Agent 的自进化 Harness Agent**


### 英文名


**Bear Agent: A Self-Evolving Harness Agent with Autonomous Session Memory Folding, Skills, MCP and Sub-Agents**

## 2. 一句话介绍


### 面试版

Bear Agent 是我从零实现的本地命令行 Harness Agent。它的核心不是简单调用大模型，而是自研 Agentic Harness：统一管理 OpenAI/Anthropic 兼容模型、工具调用、权限检查、上下文压缩、会话恢复、自主会话记忆折叠、Skills、MCP 和子 Agent；其中会话记忆折叠支持模型主动触发或系统兜底，把长对话压成结构化 session state，适合长程任务连续推进。同时项目还通过在线自进化和在线评测链路，把用户稳定反馈抽取成可复用 Skills，并用 replay、规则、LLM judge、候选变体试跑和 champion 记录评估 Skill 质量。

### 简历摘要版

基于 Python 自研 Harness Agent，统一编排 OpenAI/Anthropic 兼容模型、工具调用、权限控制、自主会话记忆折叠、Skills、MCP 与子 Agent；会话记忆折叠负责把长程会话压缩为结构化 session state，Skills 负责把稳定反馈沉淀为可复用 `SKILL.md`。同时设计在线 Skills 自进化与评测链路，通过 replay 样本、规则编译、LLM judge、candidate variants 和 champion registry 进行质量评估。

## 2.5 自主会话记忆折叠：目的与价值

### 目的

面向代码探索、调试、重构和多工具协作等长程任务，避免原始对话、工具结果和失败尝试持续堆积，导致上下文膨胀、关键信息被淹没或错误路径持续影响后续推理。

### 核心收益

- **降低上下文开销**：将冗长交互历史压缩为结构化 session state，释放上下文空间。
- **保持任务连续性**：保留整体进展、当前子目标、阻塞问题、下一步动作和工具经验，折叠后可以继续执行。
- **减少错误累积**：在重复失败或策略变化时清理噪声，让 Agent 重新审视当前状态并调整方案。
- **提升长程任务稳定性**：相比固定截断或简单摘要，三层 memory schema 能更稳定地保留任务事实、工作状态和工具使用经验。

### 简历表述

> 设计自主会话记忆折叠机制，使 Agent 能在长程任务中主动或按上下文阈值压缩历史交互，保留任务进展、当前工作状态和工具经验，并基于折叠后的结构化 session state 继续推理和执行。

## 3. 简历项目经历推荐写法

### 简历正式版

**Bear Agent：集成自主会话记忆折叠、Skills 自进化、MCP 与子 Agent 的自进化 Harness Agent**
技术栈：Python、OpenAI SDK、Anthropic SDK、MCP、JSON-RPC、Docker、Skills

- 从零实现 Agentic Harness，统一编排模型推理、tool call 解析、权限检查、工具执行、结果回写、会话恢复和上下文压缩，支持 OpenAI / Anthropic 双协议、MCP、Plan Mode 和子 Agent。
- 设计自主会话记忆折叠机制，将其作为长程任务中的状态重组和策略重启能力，而非单纯上下文压缩；通过 `episode_memory`、`working_memory`、`tool_memory` 保留任务进度、当前工作状态和工具经验，使 Agent 能在上下文受限、工具反复失败或策略变化时通过 `compact_context` 主动折叠并继续推进任务。
- 实现在线 Skills 自进化链路，通过 pending window 捕捉用户后续反馈，结合“抽取器 / 维护器”完成候选抽取、相似检索、add / merge / discard 决策和 `SKILL.md` 落盘。
- 实现 Memory + Skills 分层长期上下文，Memory 保存项目事实和用户偏好，Skills 保存可复用任务流程，并支持 BM25-lite 检索、inline / fork 执行和长期记忆召回。
- 构建 Skills 自进化质量评测机制，基于来源追踪构造 replay 样本池，结合规则评测、LLM judge、候选版本试跑和优胜版本登记，保证能力沉淀可回放、可比较、可审计。
- 基线对比：以 HiRA 为基线，在 GAIA / HLE 共 665 道长程与高难度任务上完成评测，GAIA Pass@1 从 42.1% 提升至 53.3%，HLE Pass@1 从 13.6% 提升至 20.2%。
- 消融验证：在记忆折叠消融中，关闭自主会话记忆折叠后 GAIA Pass@1 进一步降至 44.7%，验证了自主会话记忆折叠对长程任务持续推进的作用。

### 简历精简版

**Bear Agent：自进化的 Harness Agent**

- 自研 Python Agentic Harness，统一编排模型推理、工具调用、权限控制、会话恢复、自主会话记忆折叠、Skills、MCP 和子 Agent。
- 设计三层结构化会话记忆折叠机制，支持长程任务中主动压缩历史交互并继续执行，保留任务进度、当前目标和工具经验。
- 实现在线 Skills 自进化与评测闭环，从用户反馈中沉淀 `SKILL.md`，并通过 replay、LLM judge、candidate variants 和 champion registry 评估 Skill 质量。
- 在 GAIA / HLE 665 道任务上评测，分别取得 `53.3%` / `20.2%` 的 `Pass@1`；关闭会话记忆折叠后 GAIA 降至 `44.7%`。

## 4. 技术关键词

适合放在简历项目技术栈：

```text
Python, AsyncIO, Agentic Harness, Tool Calling, OpenAI-compatible API,
Anthropic-compatible API, MCP, JSON-RPC, Skills, Memory, Docker, Rich
```

适合放在项目标签：

```text
Harness Agent, Self-Evolving Agent, Skills Evolution, Local Agent Runtime,
Tool-Using Agent, MCP Client, Multi-Agent, Long-term Memory
```

中文关键词：

```text
自进化 Agent、Agentic Harness、工具调用、权限控制、长期记忆、Skills 沉淀、MCP 外部工具、子 Agent、会话恢复
```

## 5. 30 秒面试讲法

Bear Code 是我用 Python 实现的一个自进化命令行 Coding Agent。它的核心是自研 Agentic Harness，不是简单套 API，而是统一编排模型推理、工具调用、权限控制、自主会话记忆折叠、Skills、MCP 和子 Agent。最大亮点有两个：一是会话记忆可以在长对话中主动折叠，把上下文压成结构化 session state，适合长程任务；二是 Skills 自进化与评测，系统会把用户下一轮反馈作为证据沉淀成 `SKILL.md`，再用真实 replay、规则编译、LLM judge、candidate variants 和 champion 机制评估 Skill 是否稳定可用。

## 6. 1 分钟面试讲法

这个项目主要解决两个问题：第一，怎么把大模型变成能安全执行软件工程任务的本地 Agent；第二，怎么让 Agent 在使用过程中沉淀能力。  

我实现了一个 Python Agentic Harness，负责模型调用、tool call 解析、权限检查、工具执行、结果回写、会话记忆折叠和会话保存。上层支持文件读写、代码搜索、Shell、Plan Mode、OpenAI / Anthropic 双协议、Memory、Skills、MCP 和子 Agent。会话变长、工具反复失败或需要调整策略时，模型可以主动调用 `compact_context`，系统也提供上下文阈值兜底；折叠后保留任务进度、当前目标和工具经验，继续完成长程任务。

项目最核心的亮点是自进化 Skills 和在线评测。每轮回复结束后系统会保存 pending window，下一轮用户反馈会被合并为证据，再由 Extractor 抽取可复用候选，Maintainer 判断 add / merge / discard，最后写入或演化 `SKILL.md`。评测阶段会从 provenance 构造 replay，编译规则，调用 LLM judge，结合 usage stats 给出状态；如果历史回复失败，还会生成 candidate variants 重新试跑并记录 champion 产物。整个过程有权限控制、provenance 记录、版本快照和评测产物，不是无约束让模型随便改 prompt。

## 7. 2 分钟深度讲法

Bear Code 的底层是一个自研 Agentic Harness。用户输入进入 `Agent.chat()` 后，系统会初始化 MCP、检索相关 Skills、预取 Memory，然后根据配置选择 OpenAI-compatible 或 Anthropic-compatible 模型接口。模型如果返回 tool call，Harness 会先进入统一的权限检查逻辑，再分发到内置工具、Skill、MCP 工具或子 Agent，执行结果会作为 tool result 回写给模型，形成推理、行动、观察、继续推理的闭环。

在这个闭环里，我重点补了自主会话记忆折叠：当上下文变长时，模型可以主动调用 `compact_context`，系统也会在达到阈值时自动折叠，把原始对话压缩成结构化 session state 后继续执行。这样做的价值是，长任务不会因为窗口膨胀而中断，模型还能保留任务进度、工具经验和下一步动作。

我在这个闭环上做了几个扩展：第一是 Plan Mode，复杂任务先只读分析和写计划，审批后再执行；第二是 Memory 和 Skills 分层，Memory 记录事实和偏好，Skills 记录可复用方法；第三是 MCP Client，通过 stdio JSON-RPC 动态接入外部工具；第四是子 Agent，用隔离上下文处理探索、规划和局部任务。

最重要的是 Skills 自进化和在线评测。系统会把用户任务、助手回复和下一轮用户反馈组成 pending window，用 Extractor 抽取未来可复用的规则，再由 Maintainer 基于已有 Skills 做相似检索和 add / merge / discard 判断。新增或演化时会写 `SKILL.md`，记录 usage、provenance、版本快照和 retrieved / relevant / used 统计。随后 `/skill-eval` 会基于在线日志构造 replay 样本池，从 Skill 文本编译规则，使用程序检查和 LLM judge 评估历史回复，并根据失败规则生成 candidate variants 重新试跑；只有证据足够且候选优于当前版本时，才记录 champion 产物，保证自进化能力可控、可追溯、可比较。

## 8. STAR 表达

### S：背景

普通大模型工具完成任务后，经验只停留在当前上下文中；用户下次提出同类任务时仍需要重复说明偏好、格式和工作流。同时，让模型直接操作文件和 Shell 存在安全风险。

### T：目标

实现一个本地 Harness Agent，不仅能安全执行软件工程任务，还能从用户反馈中沉淀可复用能力，逐步形成项目级和用户级 Skills。

### A：行动

- 设计 Agentic Harness，统一处理模型请求、工具调用、权限检查、结果回写和会话保存。
- 设计自主会话记忆折叠机制，在上下文膨胀、重复失败或策略切换时压缩历史交互，保留关键任务状态并支持折叠后继续执行。
- 实现文件读写、精确编辑、代码搜索、Shell、Plan Mode、MCP、子 Agent 和 Skills 调用。
- 设计 Skills 自进化链路，使用 pending window 合并下一轮用户反馈，通过 Extractor 抽取候选，再由 Maintainer 判断 add / merge / discard。
- 实现 `SKILL.md` 落盘、版本 bump、演化前快照、provenance 记录、usage stats 和 stale Skill 归档。
- 实现在线 Skills 评测链路，构造 replay pool，编译 programmatic / LLM judge 规则，评估历史回复，生成 candidate variants 试跑，并通过 champion registry 记录健康版本。
- 设计会话 Memory + 长期 Memory + Skills 分层，分别承载当前任务状态、项目事实和可复用任务方法，避免不同类型的信息相互污染。

### R：结果

项目形成了一个可运行的自进化 Harness Agent：既能完成代码阅读、文件编辑、测试执行、架构分析等 Coding Agent 任务，又能通过自主会话记忆折叠稳定推进长程任务，将稳定用户反馈沉淀为可复用 Skills，并通过 replay 评测、LLM judge、candidate 试跑和 champion 产物回溯来源、版本、质量和使用效果。


## 11. 项目描述模板

### 简历项目描述

```text
Bear Agent 是一个基于 Python 的自进化命令行 Coding Agent Runtime。项目通过自研 Agentic Harness 统一编排 OpenAI/Anthropic 兼容模型、工具调用、权限控制、自主会话记忆折叠、Skills、MCP 和子 Agent；其中会话记忆折叠支持长上下文主动折叠并继续任务，Skills 则把用户稳定反馈沉淀为可复用 SKILL.md。系统还通过在线 Skills 自进化与评测闭环，将 `SKILL.md` 进一步接入 replay pool、规则编译、LLM judge、candidate variants、usage gate 和 champion registry，保证能力沉淀可审计、可回放、可对比。
```

### 最新版项目描述

```text
Bear Agent 是一个基于 Python 的自进化命令行 Coding Agent Runtime。项目通过自研 Agentic Harness 统一编排 OpenAI/Anthropic 兼容模型、工具调用、权限控制、自主会话记忆折叠、Skills、MCP 和子 Agent；同时实现在线 Skills 自进化与评测闭环，将用户稳定反馈沉淀为 SKILL.md，并通过 replay pool、规则编译、LLM judge、candidate variants、usage gate 和 champion registry 对 Skill 质量进行可审计评估。
```
